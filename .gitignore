# ── 统计数据（运行时产生，按天生成，不入库）
ai_stats-*.jsonl
*.jsonl

# ── 实时状态广播（菜单栏机器人轮询的文件，纯运行时产物）
ai_status.json

# ── 抓包报文（含明文对话内容，绝不入库）
captures/

# ── Swift 构建产物
.build/
*.app/
*.xcodeproj
*.xcworkspace
.swiftpm/
Package.resolved
DerivedData/

# ── 图标中间产物（由 make_icons.swift 生成）
*.iconset/

# ── Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── 备份与临时文件
*.bak
# 带时间戳的备份（如 ai_stats-2026-08-02.jsonl.bak.162851）——
# 上面那条 *.bak 匹配不到后缀在 .bak 之后的文件
*.bak.*
*.migrated
*.before_fix
*.log
# 轮转出来的备份（http_proxy.log.1 / .log.2，RotatingFileHandler 的 backupCount=2）——
# 上面那条 *.log 匹配不到后缀在 .log 之后的文件，同 *.bak.* 那条
*.log.*
*.tmp

# ── macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# ── 编辑器
.vscode/
.idea/
*.swp

# ── 运行时路由配置（含真实域名，本地保留）
# 参考 proxy/ai_stats_domains.example.json
proxy/ai_stats_domains.json

# ── 聚合路由的 API key（明文密钥，绝不入库）
# 参考 proxy/ai_keys.example.json
proxy/ai_keys.json
